module.exports = {
  notifyMessages: [ // array of pop up notify messages that disapear after click or timeout
    // { content: 'log message without type' }         -> $actions.notify.log(content)
    // { type: 'primary', content: 'primary message' } -> $actions.notify.primary(content)
    // { type: 'info', content: 'info message' }       -> $actions.notify.info(content)
    // { type: 'success', content: 'success message' } -> $actions.notify.success(content)
    // { type: 'warning', content: 'warning message' } -> $actions.notify.warning(content)
    // { type: 'danger', content: 'danger message' }   -> $actions.notify.danger(content)
    //
  ]
}
