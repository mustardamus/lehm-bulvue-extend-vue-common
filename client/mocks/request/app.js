module.exports = [
  /*{
    url: 'GET /articles',
    cb (req) {
      return {
        articles: [
          { id: 123, title: 'Article One' },
          { id: 123, title: 'Article Two' }
        ]
      }
    }
  },
  {
    url: 'GET /articles/:id',
    cb (req) {
      return {
        id: req.params.id,
        title: 'Article One',
        userId: req.query.userId
      }
    }
  },
  {
    url: 'POST /articles',
    cb (req) {
      if (req.headers.authorization) { // note that header names are lowercase
        return { id: 789, title: req.body.title }
      } else {
        return { status: 401 }
      }
    }
  }*/
]
